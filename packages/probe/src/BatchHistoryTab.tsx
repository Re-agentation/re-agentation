/**
 * BatchHistoryTab — shows recent 5 batches with sent time, item count,
 * ack state, files-edited count.
 *
 * Status: SCAFFOLD. See Re-agentation plan §1-F.
 */

export interface BatchHistoryTabProps {
  metroHost: string
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function BatchHistoryTab(_props: BatchHistoryTabProps): null {
  // TODO(phase-1-D): poll Metro for recent batches + ack status.
  return null
}
